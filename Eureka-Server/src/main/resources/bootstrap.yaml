spring:
  application:
    name: Eureka-Server
  cloud:
    config:
      enabled: true
      uri: http://localhost:8090

logging:
  level:
    com.netflix.eureka: DEBUG
    com.netflix.discovery: DEBUG

