spring:
  application:
    name: M1-Simulation
  cloud:
    config:
      enabled: true
      uri: http://localhost:8090
  datasource:
    url: jdbc:mysql://localhost:3306/dbtingeso
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update
  jmx:
    enabled: false
