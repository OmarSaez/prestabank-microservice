spring:
  applitation:
    name: M2-Register
  cloud:
    config:
      enabled: true
      uri: http://localhost:8090
    datasource:
      url: jdbc:mysql://${DB_HOST}:3306/dbtingeso
      username: root
      password: mysql
    jpa:
      hibernate:
        ddl-auto: update